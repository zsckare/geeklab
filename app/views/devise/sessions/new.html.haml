.card.padding-largo
  .row
    %h3.center-align
      Iniciar Sesion
  .row
    = form_for(resource, as: resource_name, url: session_path(resource_name),builder: MaterializecssFormBuilder ) do |f|
      .row
        .col.s12.m12.l4.offset-l4
          = f.label :email
          = f.email_field :email, autofocus: true

          = f.label :password , text: "Contraseña"
          = f.password_field :password, autocomplete: "off"

          -if devise_mapping.rememberable?
        
            = f.check_box :remember_me
            = f.label :remember_me
      .row
        = f.submit "Iniciar Sesion"

